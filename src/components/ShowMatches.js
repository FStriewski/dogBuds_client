import React, { PureComponent } from 'react'

export class ShowMatches extends PureComponent {

  render() {
    return (
      <button className="Button ShowMatches">
        Show my matches!
      </button>
    )
  }
}

export default ShowMatches
